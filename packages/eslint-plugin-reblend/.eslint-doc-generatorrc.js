/** @type {import('eslint-doc-generator').GenerateOptions} */
const config = {
  configEmoji: [
    ['jsx-runtime', '🏃'],
    ['recommended', '☑️'],
  ],
  ignoreConfig: ['all'],
  urlConfigs:
    'https://github.com/scyberLink/eslint-plugin-reblend/#shareable-configs',
};

module.exports = config;
